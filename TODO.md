
## 🛠 当前任务 (In Progress)

## 📝 待办事项 (Backlog)

## 💡 灵感/想法
- Windows 全局快捷键与悬浮面板
  - 说明：目前 Windows 端应该是托盘后台运行。可以增加一个全局快捷键（如 Ctrl+Shift+V 或 Alt+V），按下后在鼠标光标位置弹出一个无边框的悬浮菜单，展示最近的 ClipSync 剪贴板历史，点击或者按数字键直接输入（类似系统自带的 Win+V，但内容是跨端的）。
- 考虑用C++重写windows端
- 应用黑名单 / 白名单 (App Exclusion)：
  - 适用端：Android & Windows
  - 说明：当用户在使用特定应用（如：1Password、Bitwarden、各类银行 App）时，检测到焦点在这些 App 上，自动暂停剪贴板监控。这样可以彻底防止密码等极度敏感数据被上传到服务器。
- 端到端加密 (E2EE)：
  - 说明：客户端在上传剪贴板前使用用户设置的“主密码”进行 AES 加密，服务端只存储和转发密文（SQLite 里也是密文）。这样即使是你自己（服务器管理员）也无法偷看其他用户的剪贴板内容，极大地提升了项目的隐私信用。
- “阅后即焚” / 临时剪贴板：
  - 说明：复制验证码或单次密码时，在客户端或 Web 端提供一个标记。该条目在同步被粘贴一次后，或者 5 分钟后，自动从服务端和所有客户端销毁。
- 设备定向同步
- 链接自动净化 (URL Tracking Stripper)（强烈推荐）：
  - 说明：复制淘宝、B站、小红书、知乎等分享链接时，自动将链接后面长长的 ?spm=xxx、?utm_source=... 等追踪参数剔除，只同步最干净的短链接。
- 图片同步
- 内容自动打标签与快捷操作 (Smart Intent)：
  - 说明：如果剪贴板内容符合某种格式，自动提供对应的操作。例如：
    - 识别到 http:// 或 https:// -> Android 通知栏提供 “浏览器打开” 按钮。 *暂不考虑*
    - 识别到 6 位纯数字 -> 标记为“验证码”，甚至可以联动上一条的“阅后即焚”。
    - 识别到十六进制颜色 (#FF5733) -> Web 端展示时旁边附带一个该颜色的 预览小色块。
- 连续复制/追加模式
  - 痛点：有时候你在看一长篇文章，想摘录好几段话，需要“复制一段 -> 切换到笔记软件 -> 粘贴 -> 切回来 -> 复制下一段”，极其繁琐。
  - 功能：你可以增加一个“追加模式”快捷键（或者在 Windows 托盘开启）。开启后，你连续按多次 Ctrl+C，它不会被覆盖，而是把每次复制的内容用空行拼接成一整段大文本。最后你只需要去笔记软件里粘贴一次，就能得到所有摘录！
- 局域网优化
- 二维码/链接文本分享
- 剪贴板“撤销 / 历史回退”
  - 痛点：有时候你在电脑上复制了一段重要的密码或者长段落，准备粘贴。结果这时候你的手机不小心误触碰复制了一个空字符，或者复制了一个网址，瞬間把电脑上的剪贴板覆盖了！你辛苦写的东西没了。
  - 功能（客户端增强）：
    - 在 Windows 客户端，增加一个快捷键（比如 Ctrl+Shift+Z）。
    - 按下后，电脑当前的剪贴板内容，会自动回退到远端数据库里的上一条记录！
    - 这完全依赖现有的数据库结构（获取最近历史记录），不需要任何乱七八糟的存储。它直接解决了“跨端同步时极其容易被误覆盖”这个最大的痛点，极其硬核且实用。
- “阅后即焚” / 阅后即删 (One-Time / Burn-on-Paste)
  - (刚才提到过，但结合 FIFO 问题，这点尤为重要)
  - 不违和的接入点：这本身就是一个“管理剪贴板生命周期”的功能。
  - 场景：我只想用电脑复制验证码给手机用，用完就立刻删掉，不想让它污染我的剪贴板历史，也不想让它占用我的 50 条额度。
  - 玩法：在 Windows 托盘或手机端，增加一个“开启临时模式”或“开启阅后即删”的勾选项。开启后，发送的内容带一个特定 flag，其他设备一旦读取或成功将其写入本机的剪贴板，立刻通知服务器端将其从数据库彻底删除。既保护了隐私，又清理了空间。
- 智能粘贴板合体 (Smart Join) - 纯客户端处理
  - 我们不搞什么“草稿本”大面板，我们用极其轻量的思路来解决“多次复制，一次粘贴”的需求。
  - 功能：在客户端（Windows）增加一个钩子。
  - 用户正常使用。
  - 当用户想要合并文本时，不是去网页端整理。而是在客户端勾选“合并接下来的复制内容”。
  - 用户复制了 3 段话，系统发给了服务端 3 条独立的记录（完全符合原有的逻辑）。
  - 但是！客户端在将其写入系统的真实剪贴板时，自动把最近收到的带有“合并标识”的这 3 条，拼接成一个大字符串，塞给系统。
  - 这样用户只要 Ctrl+V 一次就能把它们贴出来。完全不影响后端的轻量架构。
- 设置是否加密传输
- 企业级多租户与频道系统 (Workspace & User Access Control)
  - 如果把它做成一个可以给整个团队、宿舍、甚至小型企业用的中枢，就需要强大的管理后台：
    - 概念：从“一个账号自己用”进化为“多账户权限控制与频道隔离”。
    - 玩法：
      - 超级管理员面板 (RBAC 面板)：可以随意封禁账号、重置密码、设置某个用户的每日最大同步次数（限流），甚至监控整个系统的活跃度大盘。
      - 频道/群组功能 (Channels)：不仅仅是私人剪贴板。可以创建类似 #运维组、#前端组 的共享剪贴板。所有加入该频道的人，只要切换到对应的剪贴板上下文，复制一条报错日志，全组人的电脑里就都有了。
      - 设备级审计与封杀：如果在 Web 后台看到某个不认识的设备 IP 在疯狂拉取历史记录，管理员可以一键“踢下线并清除该设备上的本地所有数据”（如果有客户端配合）。
- 连续性与跨端接力 (Apple Handoff 的代替品)
  - 不只是文本，我们要同步**“数字上下文（Context）”**。
  - 概念：超越纯文本，识别系统的高级意图。
  - 玩法：
    - 我在电脑上的 Chrome 看一篇知乎看到了中间，我按下一个组合任务键（调用了浏览器插件或辅助功能），将当前的**“URL + 滚动条位置”**作为一个特殊载荷写入。
    - 手机端收到这个载荷，不仅是同步了链接，还会弹出一条不可消除的通知：“📍 电脑传来进度：知乎...”。点开后直接跳到该网页的对应位置。
    - 进化：复制一个本地局域网的文件路径，另一台电脑直接用资源管理器打开该路径。
- “时光机”与跨端搜索快照 (Time Machine Local Vault)
  - 针对你担心的“服务器只有 50 条 FIFO 限制”的终极解决方案。
  - 痛点：服务端保持轻量，但用户希望能搜到半年前复制的一段长代码。
  - 玩法（端侧下放）：
    - 服务器依然只保留最后 50 条的热数据中枢。
    - 在 Windows 客户端设置一个 “永久冷备份 (Local Vault)” 开关。
    - 开启后，Windows 客户端会在本地的 ~/.clipsync/vault.db 疯狂追加每一条流经的数据，完全不占用公网服务器空间。
    - 当需要找记录时，提供一个本地的 WebUI 或者托盘搜索框，可以极速全文检索近十年来你复制过的所有百万条数据！
- 跨设备全局“文本扩展字典” (Cross-Device Text Snippets & Expansion)
  - 类似 Espanso 或 aText，但我们是跨设备、云端同步的纯血版本。
  - 概念：作为一个文字中枢，它应该能帮你“少打字”。
  - 玩法：
    - 在 Web 端配置一个“智能字典”：设置 /mail -> my.name@company.com；设置 /shrug -> ¯\_(ツ)_/¯；设置 !时间 -> 实时当前时间戳。
    - 因为你的 Windows 客户端和 Android 端本来就在后台监听键盘/剪贴板。当你敲出 /mail 时，客户端直接呼叫中枢请求扩展，瞬间把这五个字母替换成一长串正式的自我介绍或邮箱！
    - 只要在网页端改一次配置，你的手机、公司电脑、家里电脑上的“快捷短语”瞬间全部同步生效。
- 跨端 Webhook 与推送网关
  - 概念：作为剪贴板中枢，它应该能帮你“少思考”。
  - 玩法：
    - 在 Web 端配置一个“智能字典”：设置 /mail -> my.name@company.com；设置 /shrug -> ¯\_(ツ)_/¯；设置 !时间 -> 实时当前时间戳。
    - 因为你的 Windows 客户端和 Android 端本来就在后台监听键盘/剪贴板。当你敲出 /mail 时，客户端直接呼叫中枢请求扩展，瞬间把这五个字母替换成一长串正式的自我介绍或邮箱！
    - 只要在网页端改一次配置，你的手机、公司电脑、家里电脑上的“快捷短语”瞬间全部同步生效。
- 无边界隐形 AI 助手 (Invisible AI Copilot)
  - 用最符合直觉的方式召唤 AI，不需要任何 UI 界面。
  - 概念：你的剪贴板就是 AI 的对话框。
  - 神级体验：
    - 在任意界面的输入框（比如微信、Word、网页）里输入：?翻译 这是一段测试文本，或者 ?解释 error: null pointer exception。
    - 选中这段文字，按下 Ctrl+C。
    - 服务端正则拦截到 ? 前缀，不去广播它，而是直接调用 DeepSeek / OpenAI 的 API。
    - 两秒后，你会听到“叮”的一小声，服务端把 AI 的回答推回了你的剪贴板。
    - 你只需要在原位置敲下 Ctrl+V 粘贴，刚刚带问号的问题瞬间就变成了完美的答案！这是一种极其酷炫的“无感 AI”体验。
- “序列化”智能接力粘贴 (Sequenced Paste / Auth Flow)
  - 痛点：跨设备输入账号密码时，通常需要“手机复制账号 -> 电脑粘贴 -> 手机再复制密码 -> 电脑再粘贴”，来回切换极度繁琐。
  - 黑科技玩法：
    - 通过 Web 端或手机端，打包发送一个“序列 (Sequence)”: ["my_username", "my_password_123"]。
    - Windows 客户端收到序列后，首先把第一个值 my_username 放入系统剪贴板。
    - 亮点来了：客户端在后台监听系统的“粘贴事件 (Ctrl+V)”。一旦检测到用户在电脑上按下了 Ctrl+V（账号粘贴完毕），客户端瞬间自动将第二个值 my_password_123 塞进系统剪贴板！
   - 体验：你只需要在电脑的登录框上按 Ctrl+V（出账号），按 Tab 键切换输入框，再按 Ctrl+V（出密码），一气呵成，行云流水！
- 本地文件极速“液态化” (Local File to Cloud URL)
  - 让复制文件变成复制链接。
  - 痛点：有时候你想把电脑桌面的一个 report.pdf 传给手机，但剪贴板默认只能复制文本，不能直接跨网传大文件。
  - 神级体验：
    - 开启“智能文件中转”模式。
    - 当你在 Windows 里选中一个文件，按下 Ctrl+C。客户端检测到这是一个“文件路径对象”，立马在后台将文件静默上传到服务端的临时目录（比如限制单次 10MB，保留 10 分钟）。
    - 上传完成后，生成一个提取链接 http://your-server.com/d/xyz，并把它作为“文本”推送到你手机的剪贴板。
    - 手机上直接粘贴并在浏览器打开，文件立刻下载。你把笨重的文件“液态化”成了轻盈的文本流！
- 同步状态与确定性反馈 (Sync Feedback)
  - 最基础的痛点：我按了 Ctrl+C，它到底传没传到手机上？我手机复制了，电脑端到底接收成功了没？
  - 状态栏/托盘图标动画：当后台正在发送或接收数据时，Windows 右下角的托盘图标闪烁一下（或者变成绿色半秒钟）。
  - 非侵入式 Toast 提示：电脑端静默接收到手机传来的剪贴板时，在屏幕边缘（比如右下角或者鼠标指针附近）极其迅速地闪过一个小浮层：“📋 已接收：xxx...”，1 秒后消失，不打断用户操作。
  - 声音反馈：接收成功时，播放一个极其微弱、轻脆的“咔哒”声。
- 弱网无缝重传与离线队列 (Offline Queuing)
  - 最基础的痛点：手机进电梯没信号了，这时候我复制了一段文字，出电梯后它并不会自动同步给电脑，必须重新复制一次。
  - 本地暂存队列：当客户端检测到 WebSocket 断开连接时，用户复制的内容不丢弃，而是塞进本机内存的队列中。
  - 自动冲刷：一旦网络恢复，WebSocket 重新连上，客户端瞬间把队列里的历史记录“补发 (Flush)” 到服务器端，实现真正的“复制即完成，不管有网没网”。
- 富文本与多格式支持 (Rich Text Preservation)
  - 最基础的痛点：纯文本同步已经实现了，但我们复制的往往不止是纯文本。
  - 保留 HTML/RTF 格式：当在网页里复制带超链接、加粗、颜色的文本，或者在 Excel 里复制一个小表格时，客户端不仅抓取 text/plain，同时抓取 text/html。粘贴到另一台电脑的 Word 时，格式完好无损。
- 智能体积限制与截断 (Payload Throttling)
  - 最基础的痛点：不小心在电脑上复制了一个极其庞大的文件目录，或者几十万字的代码（好几 MB），直接把 WebSocket 管道卡死，甚至导致应用崩溃。
  - 阈值拦截：在客户端设定硬性限制（比如单条纯文本超 1MB 拒绝上云，并在托盘提示“内容过大，已忽略同步”）。
  - 智能截断预览：只发前 1000 个字符用于历史记录展示，避免内存暴涨。
- 跨端防环与冲突消解增强 (Enhanced Anti-Loop & Conflict Resolution)
  - 关乎基础同步的稳定性。虽然你可能已经做了防环（判断哈希是否一致），但其实还有更幽微的冲突场景。
  - 毫秒级冲突合并：如果电脑和手机在 100 毫秒内由于误触，几乎同时复制了不同的内容，到底谁覆盖谁？引入服务端的时间戳（Clock）或版本号机制，保证所有设备最终的系统剪贴板停留在最后一次真实用户的操作上，而不是反复横跳。
  - 应用层屏蔽：当你在电脑上按 Ctrl+V（粘贴）的瞬间，很多应用（如某些富文本编辑器）会在内部重写一次剪贴板，这会导致工具误以为你又“复制”了一次，从而触发无意义的全网广播。需要更聪明的“粘贴事件”过滤。
- 快捷暂停与白名单 (Quick Pause & Exclusion)（可复用现有同步方向的方法）
  - 最基础的痛点：我要开始复制一堆银行卡和密码了，我不仅不想让它们上云，我连去打开软件界面点“暂停”都嫌麻烦。
  - 一键睡眠模式：Windows 托盘右键直接“暂停同步 10 分钟”，时间一到自动恢复。
  - 进程黑名单：在 Windows 客户端配置中，填入 KeePass.exe, 1Password.exe。只要当前激活的窗口是这些程序，任何复制操作直接屏蔽，绝对不上服务器。
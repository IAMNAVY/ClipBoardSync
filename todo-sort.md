# 📋 ClipSync 进阶功能优先级规划 (todo-sort.md)

以下是对现有构想及新增脑洞的分类与优先级排序。**这里的分类和排序囊括了 `TODO.md` 中所有的灵感和想法，一个不少。** 评估依据：基础需求刚性、开发成本、以及功能的“惊艳程度（Wow-factor）”。

---

## 🚀 P0 核心体验护城河与基础设施 (近期落地重点)
**围绕“剪贴板基本功”的稳定性、反馈感与基础痛点，打造无可挑剔的日常体验。**

1. **同步状态与确定性反馈 (Sync Feedback)**
   - **说明**：Windows 托盘动画闪烁/轻微反馈音；非侵入式 Toast 提示“📋 已接收：xxx”。
2. **弱网无缝重传与离线队列 (Offline Queuing)**
   - **说明**：WebSocket 断开时不丢数据，暂存本地内存队列；网络恢复后瞬间补发 (Flush) 到服务端。
3. **智能体积限制与截断 (Payload Throttling)**
   - **说明**：单条内容超大（如 >1MB）直接拒绝上云并提示，长文本展示仅截取前 1000 字符。防止 WebSocket 管道阻塞或 OOM。
4. **跨端防环与冲突消解增强 (Enhanced Anti-Loop)**
   - **说明**：利用服务端时间戳/版本号处理毫秒级极速跨端复制冲突；智能过滤应用内部自触发的无效“粘贴重写剪贴板”事件。
5. **链接自动净化 (URL Tracking Stripper)** 🌟
   - **说明**：自动剔除淘宝、B站、知乎等分享链接后的 `?spm=xxx` 等长尾巴。
6. **剪贴板“撤销 / 历史回退”**
   - **说明**：Windows 增加 `Ctrl+Shift+Z`，一键将本机剪贴板回退为远端的上一条记录。解决辛苦复制内容被误触覆盖的问题。
7. **快捷暂停与白名单 / 应用黑名单 (Quick Pause & App Exclusion)**
   - **说明**：
     - **一键睡眠模式**：Windows 托盘右键直接“暂停同步 10 分钟”。
     - **进程应用黑白名单**：当焦点在 1Password、KeePass、各类银行 App 时，复制动作自动被拦截绝不上云（保障极度敏感数据）。
8. **“阅后即焚” / 临时剪贴板 (One-Time / Burn-on-Paste)**
   - **说明**：复制验证码或单次密码不仅不留历史，甚至可以提供标记，其他设备一旦读取或粘贴一次后，立刻通知服务器彻底销毁。

---

## ⚡ P1 中枢处理与扩展性功能 (下一阶段核心)
**由“搬运工”向“效率神器 / 文字处理器”进化。**

1. **设备定向同步**
   - **说明**：复制的特定内容可以选择“仅发送至特定设备”（如仅发给 Pixel 7手机），不再全网广播。
2. **富文本与多格式同步支持 (Rich Text Preservation)**
   - **说明**：复制文字连带其颜色、加粗、超链接（HTML/RTF 格式）一并同步。
3. **图片同步 (Image Sync)**
   - **说明**：系统级截图的图像数据二进制/Base64直接上云，让电脑截图手机秒接收成为可能。
4. **二维码 / 链接文本分享**
   - **说明**：在网页端或手机端将长文本 / URL 快速放大生成二维码，供其他人直接扫描。
5. **“序列化”智能接力粘贴 (Sequenced Paste / Auth Flow)** 🌟
   - **说明**：一次性组装发送 `[账号, 密码]`。在电脑上 `Ctrl+V` 出账号，按 `Tab` 切输入框再按 `Ctrl+V` 自动出密码，一气呵成。
6. **内容自动打标签与快捷操作 (Smart Intent)**
   - **说明**：如果识别到纯数字 -> 标记为验证码并联动阅后即焚；识别到十六进制颜色 -> 网页带颜色预览。
7. **连续复制/追加模式 & 智能粘贴板合体 (Copy-Append & Smart Join)**
   - **说明**：纯客户端处理。多次 `Ctrl+C` 自动按换行拼接，最后去目标笔记软件一次性 `Ctrl+V` 全部粘贴。
8. **跨端 Webhook 与推送网关**
   - **说明**：提供独立 API，让服务器报错、日志报警等自动化脚本可以一键把文本推送到你的各类终端。

---

## 🔒 P2 安全增强与黑科技极客探索 (高阶功能)
**安全重构与充满想象力的极客玩法。**

1. **端到端加密 (E2EE) / 设置是否加密传输**
   - **说明**：客户端在上传剪贴板前使用“主密码”进行 AES 加密，服务端只存储密文。哪怕是管理员也无法窥视其他用户的剪贴板。
2. **局域网优化 (P2P / Local Beam)**
   - **说明**：同 Wi-Fi 下终端之间自动 mDNS 发现并直连，突破公网服务器宽带瓶颈，实现真正的低保真、大体积、0延迟传输。
3. **无边界隐形 AI 助手 (Invisible AI Copilot)** 🤖
   - **说明**：复制 `?翻译 error...`，服务器正则拦截并在后台请求大模型，直接静默将答案推回目标设备剪贴板，0 UI 呼叫 AI。
4. **跨设备全局“文本扩展字典” (Cross-Device Text Snippets)**
   - **说明**：在任意设备敲出 `/mail`，自动被云端中枢替换回预设的邮箱地址。一处配置，跨端全部生效。
5. **本地文件极速“液态化” (Local File to Cloud URL)**
   - **说明**：选中本地文件 `Ctrl+C`，立马后台静默上传到服务端临时目录，其他设备收到的是一个秒开的下载短连接。
6. **Windows 全局快捷键与悬浮面板**
   - **说明**：按 `Alt+V` 在鼠标位置呼出跨端剪贴板历史列表面板（暂不考虑优先做）。

---

## 🏰 P3 长期战略与架构演进 (企业级生态 / 重构)
**重量级基础设施和系统级优化。**

1. **企业级多租户与频道系统 (Workspace & User Access Control)**
   - **说明**：全套 RBAC 权限管理，组建带频道隔离的 #研发组 的报错共享流，以及管理员全盘审计和设备拉黑。
2. **连续性与跨端接力 (Apple Handoff 的代替品)**
   - **说明**：超越纯文本。同步“当前浏览器 URL + 滚动条位置”，手机接到后弹框“📍 电脑传来进度”，一点就跳回之前的工作流。
3. **“时光机”与跨端搜索快照 (Time Machine Local Vault)**
   - **说明**：解决服务端仅 50 条的 FIFO 限制。在客户端本地保存近十年所有历史的增量 SQLite，实现离线的全文极速检索。
4. **考虑用C++重写Windows端**
   - **说明**：在 Windows 端追求极致占用和系统底层 API 调用的终极形态方案。
